{
  "devDependencies": {
    "concurrently": "^8.2.1",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.3.3"
  },
  "scripts": {
    "dev": "npx concurrently \"npx tailwindcss -i ./input.css -o ./output.css --watch\" \"npx live-server\""
  }
}
